<%= form_tag ({controller: 'sessions', action: 'create', method: 'post'}) do %>
  <%= collection_select :user, :name, User.all, :id, :name %>
  <input name=name>
   <input name=password>
  <input type=submit value='Sign In'>
<% end %>